radius = float(input("Enter the radius : "))


if 0.0 <= radius <= 100.0:
   
    pi = 3.14
    area = pi * radius ** 2
    

    print(f"Area of circle = {area:.6f}")
elif radius < 0.0 or radius > 100.0:
    
    print("Enter a positive value upto 100")# PPS-lab
